-- air.wave DELTA MOBILE v2.1 - Fixed & Improved
-- ESP: Box + Health + Skeleton, FOV Circle, Shoot-to-Aim, Mini Icon

if getgenv().AIRWAVE_DELTA_LOADED then return end
getgenv().AIRWAVE_DELTA_LOADED = true

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local localPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local camera = workspace.CurrentCamera
local TS = game:GetService("TeleportService")
local RS = game:GetService("ReplicatedStorage")
local playerGui = localPlayer:WaitForChild("PlayerGui")

-- ============================================
-- CONFIGS
-- ============================================
local ESPConfig = {Enabled = false, Boxes = true, Names = true, Distance = true, Health = true, Skeleton = false, EnemyOnly = false, MaxDist = 1000}
local AimbotConfig = {Enabled = false, TeamCheck = true, AimPart = "Head", MaxDist = 200, Smoothness = 2, AutoFire = false}
local SilentAim = {Enabled = false}
local FOVConfig = {Enabled = false, Size = 150}
local Speed = {Enabled = false, Value = 32}
local BHOP = {Enabled = false}
local Fly = {Enabled = false, Speed = 50, BodyG = nil, BodyV = nil}
local Noclip = {Enabled = false, Connection = nil}
local AFK = {Enabled = false, Map = "Arena", Weapon = "Assault Rifle"}

-- ============================================
-- FOV CIRCLE (GUI-Based for Delta)
-- ============================================
local FOVFrame = Instance.new("Frame")
FOVFrame.Size = UDim2.new(0, FOVConfig.Size * 2, 0, FOVConfig.Size * 2)
FOVFrame.Position = UDim2.new(0.5, -FOVConfig.Size, 0.5, -FOVConfig.Size)
FOVFrame.BackgroundTransparency = 1
FOVFrame.Visible = false
FOVFrame.ZIndex = 999

local FOVCircle = Instance.new("UICorner")
FOVCircle.CornerRadius = UDim.new(1, 0)
FOVCircle.Parent = FOVFrame

local FOVStroke = Instance.new("UIStroke")
FOVStroke.Thickness = 2
FOVStroke.Color = Color3.fromRGB(255, 100, 100)
FOVStroke.Parent = FOVFrame

-- ============================================
-- ESP SYSTEM (BillboardGui + Skeleton Lines)
-- ============================================
local ESPFolder = Instance.new("Folder")
ESPFolder.Name = "ESP"
ESPFolder.Parent = workspace

local SkeletonLines = {}
local ESPStorage = {}

local function IsEnemy(p)
    if not p or p == localPlayer then return false end
    if p.Team and localPlayer.Team then return p.Team ~= localPlayer.Team end
    if p.TeamColor and localPlayer.TeamColor then return p.TeamColor ~= localPlayer.TeamColor end
    return true
end

local function CreateESP(player)
    if ESPStorage[player] then return end
    
    local bb = Instance.new("BillboardGui")
    bb.Name = player.Name
    bb.Size = UDim2.new(0, 120, 0, 60)
    bb.StudsOffset = Vector3.new(0, 3, 0)
    bb.AlwaysOnTop = true
    bb.Parent = ESPFolder
    
    local box = Instance.new("Frame")
    box.Name = "Box"
    box.Size = UDim2.new(1, 0, 0, 45)
    box.Position = UDim2.new(0, 0, 0, 15)
    box.BackgroundTransparency = 0.9
    box.BorderSizePixel = 2
    box.Parent = bb
    
    local name = Instance.new("TextLabel")
    name.Size = UDim2.new(1, 0, 0, 14)
    name.BackgroundTransparency = 1
    name.Text = player.DisplayName
    name.TextColor3 = Color3.new(1, 1, 1)
    name.TextStrokeTransparency = 0
    name.TextSize = 10
    name.Font = Enum.Font.GothamBold
    name.Parent = bb
    
    local dist = Instance.new("TextLabel")
    dist.Size = UDim2.new(1, 0, 0, 10)
    dist.Position = UDim2.new(0, 0, 0, 62)
    dist.BackgroundTransparency = 1
    dist.Text = "0m"
    dist.TextColor3 = Color3.fromRGB(200, 200, 200)
    dist.TextStrokeTransparency = 0
    dist.TextSize = 9
    dist.Font = Enum.Font.Gotham
    dist.Parent = bb
    
    local hpBG = Instance.new("Frame")
    hpBG.Size = UDim2.new(1, -6, 0, 4)
    hpBG.Position = UDim2.new(0, 3, 0, 48)
    hpBG.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    hpBG.BorderSizePixel = 0
    hpBG.Parent = bb
    
    local hp = Instance.new("Frame")
    hp.Size = UDim2.new(1, 0, 1, 0)
    hp.BackgroundColor3 = Color3.fromRGB(60, 220, 60)
    hp.BorderSizePixel = 0
    hp.Parent = hpBG
    
    ESPStorage[player] = {Billboard = bb, Box = box, Name = name, Dist = dist, HP = hp, HPBG = hpBG}
end

local function CreateSkeletonLine()
    local line = Instance.new("Frame")
    line.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
    line.BorderSizePixel = 0
    line.Visible = false
    line.Parent = playerGui
    return line
end

local function UpdateSkeleton(player, color)
    if not ESPConfig.Skeleton then
        if SkeletonLines[player] then
            for _, line in pairs(SkeletonLines[player]) do
                line:Destroy()
            end
            SkeletonLines[player] = nil
        end
        return
    end
    
    local char = player.Character
    if not char then return end
    
    local parts = {
        {char:FindFirstChild("Head"), char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso")},
        {char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso"), char:FindFirstChild("LowerTorso") or char:FindFirstChild("HumanoidRootPart")},
        {char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso"), char:FindFirstChild("LeftUpperArm") or char:FindFirstChild("Left Arm")},
        {char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso"), char:FindFirstChild("RightUpperArm") or char:FindFirstChild("Right Arm")},
        {char:FindFirstChild("LowerTorso") or char:FindFirstChild("HumanoidRootPart"), char:FindFirstChild("LeftUpperLeg") or char:FindFirstChild("Left Leg")},
        {char:FindFirstChild("LowerTorso") or char:FindFirstChild("HumanoidRootPart"), char:FindFirstChild("RightUpperLeg") or char:FindFirstChild("Right Leg")},
    }
    
    if not SkeletonLines[player] then
        SkeletonLines[player] = {}
        for i = 1, 6 do
            SkeletonLines[player][i] = CreateSkeletonLine()
        end
    end
    
    for i, line in pairs(SkeletonLines[player]) do
        local connection = parts[i]
        if connection and connection[1] and connection[2] then
            local p1, onScreen1 = camera:WorldToViewportPoint(connection[1].Position)
            local p2, onScreen2 = camera:WorldToViewportPoint(connection[2].Position)
            
            if onScreen1 and onScreen2 then
                local distance = (Vector2.new(p1.X, p1.Y) - Vector2.new(p2.X, p2.Y)).Magnitude
                local angle = math.atan2(p2.Y - p1.Y, p2.X - p1.X) * (180 / math.pi)
                
                line.Size = UDim2.new(0, distance, 0, 1.5)
                line.Position = UDim2.new(0, (p1.X + p2.X) / 2, 0, (p1.Y + p2.Y) / 2)
                line.Rotation = angle
                line.Visible = ESPConfig.Enabled and ESPConfig.Skeleton
                line.BackgroundColor3 = color
            else
                line.Visible = false
            end
        else
            line.Visible = false
        end
    end
end

local function RemoveESP(player)
    if ESPStorage[player] then
        if ESPStorage[player].Billboard then ESPStorage[player].Billboard:Destroy() end
        ESPStorage[player] = nil
    end
    if SkeletonLines[player] then
        for _, line in pairs(SkeletonLines[player]) do
            line:Destroy()
        end
        SkeletonLines[player] = nil
    end
end

RunService.RenderStepped:Connect(function()
    FOVFrame.Visible = FOVConfig.Enabled
    FOVFrame.Size = UDim2.new(0, FOVConfig.Size * 2, 0, FOVConfig.Size * 2)
    FOVFrame.Position = UDim2.new(0.5, -FOVConfig.Size, 0.5, -FOVConfig.Size)
    
    for _, p in pairs(Players:GetPlayers()) do
        if p == localPlayer then continue end
        
        if not ESPConfig.Enabled then
            RemoveESP(p)
            continue
        end
        
        local isEnemy = IsEnemy(p)
        if ESPConfig.EnemyOnly and not isEnemy then
            RemoveESP(p)
            continue
        end
        
        local char = p.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if not char or not root or not hum or hum.Health <= 0 then
            RemoveESP(p)
            continue
        end
        
        if not ESPStorage[p] then CreateESP(p) end
        local esp = ESPStorage[p]
        
        esp.Billboard.Adornee = root
        
        local myRoot = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
        local dist = myRoot and (root.Position - myRoot.Position).Magnitude or 0
        if dist > ESPConfig.MaxDist then
            esp.Billboard.Enabled = false
            UpdateSkeleton(p, isEnemy and Color3.fromRGB(255, 60, 60) or Color3.fromRGB(60, 255, 100))
            continue
        end
        
        esp.Billboard.Enabled = true
        local color = isEnemy and Color3.fromRGB(255, 60, 60) or Color3.fromRGB(60, 255, 100)
        
        esp.Box.Visible = ESPConfig.Boxes
        esp.Box.BackgroundColor3 = color
        esp.Box.BorderColor3 = color
        
        esp.Name.Visible = ESPConfig.Names
        esp.Name.TextColor3 = color
        
        esp.Dist.Visible = ESPConfig.Distance
        esp.Dist.Text = string.format("%.0fm", dist)
        
        esp.HPBG.Visible = ESPConfig.Health
        esp.HP.Visible = ESPConfig.Health
        local pct = hum.Health / hum.MaxHealth
        esp.HP.Size = UDim2.new(pct, 0, 1, 0)
        esp.HP.BackgroundColor3 = Color3.fromRGB(255 * (1-pct), 255 * pct, 60)
        
        UpdateSkeleton(p, color)
    end
end)

Players.PlayerRemoving:Connect(RemoveESP)

-- ============================================
-- AIMBOT - ONLY WHEN SHOOTING
-- ============================================
local IsShooting = false
local function GetClosest()
    local closest, shortest = nil, math.huge
    local center = Vector2.new(camera.ViewportSize.X/2, camera.ViewportSize.Y/2)
    local camPos = camera.CFrame.Position
    
    for _, p in pairs(Players:GetPlayers()) do
        if p == localPlayer then continue end
        if AimbotConfig.TeamCheck and not IsEnemy(p) then continue end
        
        local part = p.Character and p.Character:FindFirstChild(AimbotConfig.AimPart)
        if not part then continue end
        
        local worldDist = (part.Position - camPos).Magnitude
        if worldDist > AimbotConfig.MaxDist then continue end
        
        if FOVConfig.Enabled then
            local screenPos, onScreen = camera:WorldToViewportPoint(part.Position)
            if not onScreen then continue end
            local distFromCenter = (Vector2.new(screenPos.X, screenPos.Y) - center).Magnitude
            if distFromCenter > FOVConfig.Size then continue end
        end
        
        local pos, vis = camera:WorldToViewportPoint(part.Position)
        if not vis then continue end
        
        local screenDist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
        if screenDist < shortest then
            shortest = screenDist
            closest = part
        end
    end
    return closest
end

RunService.RenderStepped:Connect(function()
    if AimbotConfig.Enabled and IsShooting then
        local target = GetClosest()
        if target then
            local pos = camera:WorldToViewportPoint(target.Position)
            local dx = (pos.X - camera.ViewportSize.X/2) / AimbotConfig.Smoothness
            local dy = (pos.Y - camera.ViewportSize.Y/2) / AimbotConfig.Smoothness
            pcall(function() if mousemoverel then mousemoverel(dx, dy) end end)
        end
    end
    
    if SilentAim.Enabled and IsShooting then
        local closest, shortest = nil, math.huge
        local center = Vector2.new(camera.ViewportSize.X/2, camera.ViewportSize.Y/2)
        
        for _, p in pairs(Players:GetPlayers()) do
            if p == localPlayer then continue end
            local head = p.Character and p.Character:FindFirstChild("Head")
            if not head then continue end
            local pos, vis = camera:WorldToViewportPoint(head.Position)
            if not vis then continue end
            
            if FOVConfig.Enabled then
                local distFromCenter = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                if distFromCenter > FOVConfig.Size then continue end
            end
            
            local dist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
            if dist < shortest and dist < 250 then
                shortest = dist
                closest = head
            end
        end
        
        if closest then
            pcall(function() camera.CFrame = CFrame.new(camera.CFrame.Position, closest.Position) end)
        end
    end
end)

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        IsShooting = true
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        IsShooting = false
    end
end)

-- ============================================
-- MOVEMENT
-- ============================================
RunService.Heartbeat:Connect(function()
    if Speed.Enabled and localPlayer.Character then
        local hum = localPlayer.Character:FindFirstChildOfClass("Humanoid")
        local root = localPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hum and root and hum.MoveDirection.Magnitude > 0 then
            root.Velocity = hum.MoveDirection * Speed.Value + Vector3.new(0, root.Velocity.Y, 0)
        end
    end
    
    if BHOP.Enabled and localPlayer.Character then
        local hum = localPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum:GetState() == Enum.HumanoidStateType.Landed then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
    
    if Fly.Enabled and Fly.BodyV and Fly.BodyG then
        local vel = Vector3.new(0, 0, 0)
        local cf = camera.CFrame
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then vel = vel + cf.LookVector * Fly.Speed end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then vel = vel - cf.LookVector * Fly.Speed end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then vel = vel - cf.RightVector * Fly.Speed end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then vel = vel + cf.RightVector * Fly.Speed end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then vel = vel + Vector3.new(0, Fly.Speed, 0) end
        Fly.BodyV.Velocity = vel
        Fly.BodyG.CFrame = cf
    end
    
    if Noclip.Enabled and localPlayer.Character then
        for _, p in pairs(localPlayer.Character:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide = false end
        end
    end
end)

spawn(function()
    while wait(1) do
        if AFK.Enabled then
            pcall(function() RS.Remotes.Matchmaking.JoinQueue:InvokeServer("1v1") end)
            pcall(function() RS.Remotes.Duels.VoteMap:FireServer(AFK.Map) end)
            pcall(function() RS.Remotes.Replication.Fighter.PickWeapons:FireServer({AFK.Weapon, "Handgun", "Fists"}) end)
        end
    end
end)

-- ============================================
-- COMPACT UI WITH TABS + MINI ICON
-- ============================================
local function CreateUI()
    for _, g in pairs(playerGui:GetChildren()) do
        if g.Name == "AirwaveDelta" then g:Destroy() end
    end
    
    local gui = Instance.new("ScreenGui")
    gui.Name = "AirwaveDelta"
    gui.Parent = playerGui
    gui.ResetOnSpawn = false
    gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    
    FOVFrame.Parent = gui
    
    local main = Instance.new("Frame")
    main.Size = UDim2.new(0, 200, 0, 280)
    main.Position = UDim2.new(0, 10, 0.5, -140)
    main.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    main.BorderSizePixel = 0
    main.Active = true
    main.Draggable = true
    main.Parent = gui
    Instance.new("UICorner", main).CornerRadius = UDim.new(0, 8)
    
    local header = Instance.new("Frame")
    header.Size = UDim2.new(1, 0, 0, 30)
    header.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    header.Parent = main
    Instance.new("UICorner", header).CornerRadius = UDim.new(0, 8)
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -60, 1, 0)
    title.Position = UDim2.new(0, 8, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "Airwave"
    title.TextColor3 = Color3.new(1, 1, 1)
    title.TextSize = 14
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = header
    
    local close = Instance.new("TextButton")
    close.Size = UDim2.new(0, 24, 0, 24)
    close.Position = UDim2.new(1, -28, 0, 3)
    close.BackgroundColor3 = Color3.fromRGB(180, 60, 60)
    close.Text = "X"
    close.TextColor3 = Color3.new(1, 1, 1)
    close.TextSize = 12
    close.Font = Enum.Font.GothamBold
    close.Parent = header
    Instance.new("UICorner", close).CornerRadius = UDim.new(0, 4)
    
    local tabBar = Instance.new("Frame")
    tabBar.Size = UDim2.new(1, -10, 0, 28)
    tabBar.Position = UDim2.new(0, 5, 0, 32)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    
    local tabList = Instance.new("UIListLayout")
    tabList.FillDirection = Enum.FillDirection.Horizontal
    tabList.SortOrder = Enum.SortOrder.LayoutOrder
    tabList.Padding = UDim.new(0, 4)
    tabList.Parent = tabBar
    
    local content = Instance.new("Frame")
    content.Size = UDim2.new(1, -10, 1, -65)
    content.Position = UDim2.new(0, 5, 0, 62)
    content.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    content.BorderSizePixel = 0
    content.Parent = main
    Instance.new("UICorner", content).CornerRadius = UDim.new(0, 6)
    
    local pages = {}
    local tabs = {}
    local toggleRefs = {}
    
    local function CreateTab(name)
        local tab = Instance.new("TextButton")
        tab.Size = UDim2.new(0, 44, 1, 0)
        tab.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
        tab.Text = name
        tab.TextColor3 = Color3.fromRGB(180, 180, 180)
        tab.TextSize = 10
        tab.Font = Enum.Font.GothamBold
        tab.Parent = tabBar
        Instance.new("UICorner", tab).CornerRadius = UDim.new(0, 4)
        
        local page = Instance.new("ScrollingFrame")
        page.Size = UDim2.new(1, -8, 1, -8)
        page.Position = UDim2.new(0, 4, 0, 4)
        page.BackgroundTransparency = 1
        page.ScrollBarThickness = 4
        page.ScrollingEnabled = true
        page.CanvasSize = UDim2.new(0, 0, 0, 400)
        page.Visible = false
        page.Parent = content
        
        local list = Instance.new("UIListLayout")
        list.Padding = UDim.new(0, 4)
        list.Parent = page
        
        list:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            page.CanvasSize = UDim2.new(0, 0, 0, list.AbsoluteContentSize.Y + 20)
        end)
        
        table.insert(tabs, tab)
        table.insert(pages, page)
        
        tab.MouseButton1Click:Connect(function()
            for _, t in ipairs(tabs) do
                t.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
                t.TextColor3 = Color3.fromRGB(180, 180, 180)
            end
            for _, p in ipairs(pages) do
                p.Visible = false
            end
            tab.BackgroundColor3 = Color3.fromRGB(80, 120, 180)
            tab.TextColor3 = Color3.new(1, 1, 1)
            page.Visible = true
        end)
        
        if #tabs == 1 then
            tab.BackgroundColor3 = Color3.fromRGB(80, 120, 180)
            tab.TextColor3 = Color3.new(1, 1, 1)
            page.Visible = true
        end
        
        return page
    end
    
    local aimPage = CreateTab("Aim")
    local espPage = CreateTab("ESP")
    local miscPage = CreateTab("Misc")
    local farmPage = CreateTab("AFK")
    
    local function AddToggle(page, text, default, callback, id)
        local f = Instance.new("Frame")
        f.Size = UDim2.new(1, 0, 0, 26)
        f.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        f.Parent = page
        Instance.new("UICorner", f).CornerRadius = UDim.new(0, 4)
        
        local l = Instance.new("TextLabel")
        l.Size = UDim2.new(0.6, 0, 1, 0)
        l.Position = UDim2.new(0, 6, 0, 0)
        l.BackgroundTransparency = 1
        l.Text = text
        l.TextColor3 = Color3.new(1, 1, 1)
        l.TextSize = 10
        l.Font = Enum.Font.Gotham
        l.TextXAlignment = Enum.TextXAlignment.Left
        l.Parent = f
        
        local t = Instance.new("TextButton")
        t.Size = UDim2.new(0, 36, 0, 20)
        t.Position = UDim2.new(1, -40, 0.5, -10)
        t.BackgroundColor3 = default and Color3.fromRGB(60, 180, 80) or Color3.fromRGB(100, 100, 100)
        t.Text = default and "ON" or "OFF"
        t.TextColor3 = Color3.new(1, 1, 1)
        t.TextSize = 9
        t.Font = Enum.Font.GothamBold
        t.Parent = f
        Instance.new("UICorner", t).CornerRadius = UDim.new(0, 4)
        
        if id then toggleRefs[id] = t end
        
        local s = default
        t.MouseButton1Click:Connect(function()
            s = not s
            t.BackgroundColor3 = s and Color3.fromRGB(60, 180, 80) or Color3.fromRGB(100, 100, 100)
            t.Text = s and "ON" or "OFF"
            if callback then callback(s) end
        end)
    end
    
    local function AddSlider(page, text, min, def, max, callback)
        local f = Instance.new("Frame")
        f.Size = UDim2.new(1, 0, 0, 40)
        f.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        f.Parent = page
        Instance.new("UICorner", f).CornerRadius = UDim.new(0, 4)
        
        local l = Instance.new("TextLabel")
        l.Size = UDim2.new(1, -10, 0, 14)
        l.Position = UDim2.new(0, 6, 0, 4)
        l.BackgroundTransparency = 1
        l.Text = text .. ": " .. def
        l.TextColor3 = Color3.new(1, 1, 1)
        l.TextSize = 9
        l.Font = Enum.Font.Gotham
        l.TextXAlignment = Enum.TextXAlignment.Left
        l.Parent = f
        
        local bg = Instance.new("TextButton")
        bg.Size = UDim2.new(1, -12, 0, 16)
        bg.Position = UDim2.new(0, 6, 0, 20)
        bg.BackgroundColor3 = Color3.fromRGB(60, 60, 75)
        bg.Text = ""
        bg.Parent = f
        Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 3)
        
        local fill = Instance.new("Frame")
        fill.Size = UDim2.new((def-min)/(max-min), 0, 1, 0)
        fill.BackgroundColor3 = Color3.fromRGB(100, 160, 220)
        fill.Parent = bg
        Instance.new("UICorner", fill).CornerRadius = UDim.new(0, 3)
        
        bg.MouseButton1Click:Connect(function(x)
            local p = math.clamp((x - bg.AbsolutePosition.X) / bg.AbsoluteSize.X, 0, 1)
            local v = math.floor(min + (max - min) * p)
            fill.Size = UDim2.new(p, 0, 1, 0)
            l.Text = text .. ": " .. v
            if callback then callback(v) end
        end)
    end
    
    local function AddDropdown(page, text, opts, def, callback)
        local f = Instance.new("Frame")
        f.Size = UDim2.new(1, 0, 0, 26)
        f.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        f.ClipsDescendants = true
        f.Parent = page
        Instance.new("UICorner", f).CornerRadius = UDim.new(0, 4)
        
        local l = Instance.new("TextLabel")
        l.Size = UDim2.new(0.4, 0, 0, 26)
        l.Position = UDim2.new(0, 6, 0, 0)
        l.BackgroundTransparency = 1
        l.Text = text
        l.TextColor3 = Color3.new(1, 1, 1)
        l.TextSize = 9
        l.Font = Enum.Font.Gotham
        l.TextXAlignment = Enum.TextXAlignment.Left
        l.Parent = f
        
        local d = Instance.new("TextButton")
        d.Size = UDim2.new(0.6, -8, 0, 20)
        d.Position = UDim2.new(0.4, 4, 0.5, -10)
        d.BackgroundColor3 = Color3.fromRGB(60, 60, 75)
        d.Text = def
        d.TextColor3 = Color3.new(1, 1, 1)
        d.TextSize = 9
        d.Font = Enum.Font.Gotham
        d.Parent = f
        Instance.new("UICorner", d).CornerRadius = UDim.new(0, 3)
        
        local expanded = false
        d.MouseButton1Click:Connect(function()
            expanded = not expanded
            if expanded then
                f.Size = UDim2.new(1, 0, 0, 26 + #opts * 22)
                for i, opt in ipairs(opts) do
                    local b = Instance.new("TextButton")
                    b.Size = UDim2.new(0.6, -8, 0, 20)
                    b.Position = UDim2.new(0.4, 4, 0, 26 + (i-1)*22)
                    b.BackgroundColor3 = Color3.fromRGB(70, 70, 85)
                    b.Text = opt
                    b.TextColor3 = Color3.new(1, 1, 1)
                    b.TextSize = 9
                    b.Font = Enum.Font.Gotham
                    b.Parent = f
                    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 3)
                    b.MouseButton1Click:Connect(function()
                        d.Text = opt
                        expanded = false
                        f.Size = UDim2.new(1, 0, 0, 26)
                        for _, c in pairs(f:GetChildren()) do
                            if c:IsA("TextButton") and c ~= d then c:Destroy() end
                        end
                        if callback then callback(opt) end
                    end)
                end
            else
                f.Size = UDim2.new(1, 0, 0, 26)
                for _, c in pairs(f:GetChildren()) do
                    if c:IsA("TextButton") and c ~= d then c:Destroy() end
                end
            end
        end)
    end
    
    local function AddButton(page, text, callback)
        local b = Instance.new("TextButton")
        b.Size = UDim2.new(1, 0, 0, 26)
        b.BackgroundColor3 = Color3.fromRGB(50, 90, 150)
        b.Text = text
        b.TextColor3 = Color3.new(1, 1, 1)
        b.TextSize = 10
        b.Font = Enum.Font.GothamBold
        b.Parent = page
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 4)
        b.MouseButton1Click:Connect(function() if callback then callback() end end)
    end
    
    -- AIM TAB - with mutual exclusion
    AddToggle(aimPage, "Aimbot", false, function(v)
        AimbotConfig.Enabled = v
        if v and SilentAim.Enabled then
            SilentAim.Enabled = false
            if toggleRefs["silentaim"] then
                toggleRefs["silentaim"].BackgroundColor3 = Color3.fromRGB(100, 100, 100)
                toggleRefs["silentaim"].Text = "OFF"
            end
        end
    end)
    AddToggle(aimPage, "Silent Aim", false, function(v)
        SilentAim.Enabled = v
        if v and AimbotConfig.Enabled then
            AimbotConfig.Enabled = false
            if toggleRefs["aimbot"] then
                toggleRefs["aimbot"].BackgroundColor3 = Color3.fromRGB(100, 100, 100)
                toggleRefs["aimbot"].Text = "OFF"
            end
        end
    end, "silentaim")
    AddToggle(aimPage, "FOV Circle", false, function(v) FOVConfig.Enabled = v end)
    AddSlider(aimPage, "FOV Size", 50, 150, 400, function(v) FOVConfig.Size = v end)
    AddToggle(aimPage, "Team Check", true, function(v) AimbotConfig.TeamCheck = v end)
    AddToggle(aimPage, "Auto Fire", false, function(v) AimbotConfig.AutoFire = v end)
    AddDropdown(aimPage, "Part", {"Head", "Torso", "HumanoidRootPart"}, "Head", function(v) AimbotConfig.AimPart = v end)
    AddSlider(aimPage, "Max Dist", 50, 200, 1000, function(v) AimbotConfig.MaxDist = v end)
    AddSlider(aimPage, "Smooth", 1, 2, 10, function(v) AimbotConfig.Smoothness = v end)
    
    -- ESP TAB
    AddToggle(espPage, "ESP", false, function(v) ESPConfig.Enabled = v end)
    AddToggle(espPage, "Boxes", true, function(v) ESPConfig.Boxes = v end)
    AddToggle(espPage, "Names", true, function(v) ESPConfig.Names = v end)
    AddToggle(espPage, "Distance", true, function(v) ESPConfig.Distance = v end)
    AddToggle(espPage, "Health", true, function(v) ESPConfig.Health = v end)
    AddToggle(espPage, "Skeleton", false, function(v) ESPConfig.Skeleton = v end)
    AddToggle(espPage, "Enemy Only", false, function(v) ESPConfig.EnemyOnly = v end)
    AddSlider(espPage, "Max Dist", 100, 1000, 5000, function(v) ESPConfig.MaxDist = v end)
    
    -- MISC TAB
    AddToggle(miscPage, "Speed", false, function(v) Speed.Enabled = v end)
    AddSlider(miscPage, "Speed Val", 16, 32, 300, function(v) Speed.Value = v end)
    AddToggle(miscPage, "BHOP", false, function(v) BHOP.Enabled = v end)
    AddToggle(miscPage, "Fly", false, function(v)
        Fly.Enabled = v
        if v then
            local root = localPlayer.Character and localPlayer.Character:FindFirstChild("HumanoidRootPart")
            if root then
                Fly.BodyG = Instance.new("BodyGyro")
                Fly.BodyG.P = 9e4
                Fly.BodyG.MaxTorque = Vector3.new(9e9, 9e9, 9e9)
                Fly.BodyG.CFrame = root.CFrame
                Fly.BodyG.Parent = root
                Fly.BodyV = Instance.new("BodyVelocity")
                Fly.BodyV.Velocity = Vector3.new(0, 0, 0)
                Fly.BodyV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
                Fly.BodyV.Parent = root
            end
        else
            if Fly.BodyG then Fly.BodyG:Destroy() end
            if Fly.BodyV then Fly.BodyV:Destroy() end
        end
    end)
    AddSlider(miscPage, "Fly Speed", 10, 50, 200, function(v) Fly.Speed = v end)
    AddToggle(miscPage, "NoClip", false, function(v)
        Noclip.Enabled = v
        if v then
            Noclip.Connection = RunService.Stepped:Connect(function()
                for _, p in pairs(localPlayer.Character:GetDescendants()) do
                    if p:IsA("BasePart") then p.CanCollide = false end
                end
            end)
        else
            if Noclip.Connection then Noclip.Connection:Disconnect() end
        end
    end)
    AddButton(miscPage, "Rejoin", function() TS:Teleport(17625359962, localPlayer) end)
    
    -- AFK TAB
    AddToggle(farmPage, "AFK Farm", false, function(v) AFK.Enabled = v end)
    AddDropdown(farmPage, "Map", {"Arena", "Ruins", "Construction", "Desert", "Warehouse"}, "Arena", function(v) AFK.Map = v end)
    AddDropdown(farmPage, "Weapon", {"Assault Rifle", "Handgun", "Fists", "Shotgun", "SMG"}, "Assault Rifle", function(v) AFK.Weapon = v end)
    
    -- Mini Icon when closed
    local miniIcon = nil
    
    close.MouseButton1Click:Connect(function()
        main.Visible = false
        
        if not miniIcon then
            miniIcon = Instance.new("TextButton")
            miniIcon.Size = UDim2.new(0, 40, 0, 40)
            miniIcon.Position = UDim2.new(0, 10, 0.5, -20)
            miniIcon.BackgroundColor3 = Color3.fromRGB(80, 120, 180)
            miniIcon.Text = "☰"
            miniIcon.TextColor3 = Color3.new(1, 1, 1)
            miniIcon.TextSize = 18
            miniIcon.Font = Enum.Font.GothamBold
            miniIcon.Parent = gui
            miniIcon.Active = true
            miniIcon.Draggable = true
            Instance.new("UICorner", miniIcon).CornerRadius = UDim.new(1, 0)
            
            miniIcon.MouseButton1Click:Connect(function()
                main.Visible = true
                miniIcon.Visible = false
            end)
        end
        miniIcon.Visible = true
    end)
end

if localPlayer.Character then CreateUI() end
localPlayer.CharacterAdded:Connect(function() wait(1) CreateUI() end)
